# dist

Ten folder jest miejscem na lokalnie generowane paczki release.

Pliki ZIP generowane przez `tools/build_release_zip.py` są ignorowane przez Git i nie powinny być commitowane do repozytorium jako zwykłe pliki.

Gotową paczkę release najlepiej dodać jako asset w GitHub Releases, np. przy tagu `v0.1.0`.